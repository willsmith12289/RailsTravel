<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Travel</title>

  <%= csrf_meta_tags %>
  <%= Gon::Base.render_data %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application' %>


</head>
<body>
  <nav class="navbar fixed-top navbar-toggleable-lg navbar-inverse bg-inverse">
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <span class="navbar-text"><%= link_to "Sign Up", new_user_registration_path(@user), class: "btn btn-inverse" %></span>
        <span class="navbar-text"><%= link_to "Sign In", new_user_session_path(@user), class: "btn btn-inverse" %></span>
        <span class="navbar-text"><%= link_to "Sign Out", destroy_user_session_path(@user), method: :delete, class: "btn btn-inverse" %></span>
        <% if user_signed_in? %>
          <span class="navbar-text"><%= current_user.email %></span>
        <% else %>
          <span></span>
        <% end %>
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
      </div>
    </div>
  </nav>

<%= yield %>


</body>
</html>
